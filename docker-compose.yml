services:
  layerwatch:
    image: zhf883680/layerwatch:latest
    container_name: layerwatch
    restart: unless-stopped
    pull_policy: always
    ports:
      - "19091:19091"
    volumes:
      - ./data:/app/data
    environment:
      - TZ=Asia/Shanghai
